import React from "react"
import ReactDOM from "react-dom/client"
import RaceReadyDashboard from "../main/resources/templates/result"
import "./styles.css"

ReactDOM.createRoot(document.getElementById("root")!).render(
    <React.StrictMode>
        <RaceReadyDashboard />
    </React.StrictMode>
)
